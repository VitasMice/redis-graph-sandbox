To start project run in terminal:
```bash
docker-compose up -d
```
Enter to container:
```bash
docker exec -it php_sandbox sh
```
And run composer:
```bash
composer install
```

To stop project run in terminal:
```bash
docker-compose down
```